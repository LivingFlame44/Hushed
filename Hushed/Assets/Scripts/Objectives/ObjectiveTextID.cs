using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectiveTextID : MonoBehaviour
{
    // Start is called before the first frame update
    public int objectiveID;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
